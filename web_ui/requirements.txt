# 本番 (Cloud Run) 用ランタイム依存パッケージ
# バージョンはローカル検証済みのものに固定し、Cloud Run ビルドの再現性を確保する。
# 開発・テスト用パッケージは requirements-dev.txt を参照。

fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
python-multipart==0.0.32

# Gemini (Vertex AI) + Google Search Grounding
google-genai==2.23.0
google-cloud-aiplatform==2.1.0

# BigQuery ロードジョブ
google-cloud-bigquery==3.45.0

# 認証 (Cloud Run では ADC = アタッチしたサービスアカウントを自動利用)
google-auth==2.58.0
